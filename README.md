# PS-MailboxForwardingAudit
Finds who configured email forwarding on an Exchange Online mailbox and when. Searches the Unified Audit Log using both email and Object ID — because GUI changes don't log the way you think they do.
